class <%= migration_class_name %> < ActiveRecord::Migration
  def change
<%- @keys.each do |table, columns| -%>
  <%- columns.each do |column| -%>
    add_foreign_key :<%= table %>, :<%= column.sub("_id", "").pluralize %>
  <%- end -%>
<%- end -%>
  end
end
